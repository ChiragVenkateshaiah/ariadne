# payment-svc is reachable from booking-api (each has a CALLS edge to it in the World Model) and from nothing else.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-payment-svc-from-callers
  namespace: travel
  labels:
    ariadne.dev/generated: 'true'
spec:
  podSelector:
    matchLabels:
      app: payment-svc
  policyTypes:
  - Ingress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: booking-api
